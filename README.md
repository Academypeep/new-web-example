# PKG Portfolio Frontend

Scaffolded React + TypeScript + Vite frontend for the interactive CV/portfolio.

Quick start:

```bash
cd pkg-portfolio-frontend
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

This project uses Tailwind CSS and Chart.js (via `react-chartjs-2`).
